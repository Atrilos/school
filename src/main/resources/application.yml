server:
  port: 8080

spring:
  datasource:
    url: ***REMOVED***
    username: student
    password: ***REMOVED***
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
  jackson:
      serialization:
        indent-output: true
