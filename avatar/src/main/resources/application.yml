server:
  port: 8082

spring:
  datasource:
    url: ***REMOVED***
    username: student
    password: ***REMOVED***
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
  jackson:
    serialization:
      indent-output: true
  servlet:
    multipart:
      max-file-size: 10MB
#Constants
path:
  to:
    avatars:
      folder: ./avatars